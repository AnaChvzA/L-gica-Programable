module contador #(parameter CMAX=1_000_000)(
    input clk,
    input rst,
    output reg [25:0] count
);

always @(posedge clk or posedge rst)
    if (rst)  
        count <=0;
    begin
		if (count==CMAX)
		count <=0;
		else 
        count <= count +1;
    end 
endmodule